apiVersion: v1
kind: Pod
metadata:
  labels:
    app-shell-pod: app-shell
  name: app-shell
spec:
  volumes:
    - name: app-shell-pv-storage
      persistentVolumeClaim:
        claimName: local-pv-volume-claim
  containers:
    - image: adityasingh1991/microservices_onprem:app-shell
      name: app-shell
      imagePullPolicy: IfNotPresent
      ports:
        - containerPort: 443
          protocol: TCP
      volumeMounts:
        - mountPath: /etc/ssl/certs/localhost.crt
          name: app-shell-pv-storage
          subPath: Frontend-Https-Certificates/localhost.crt
        - mountPath: /etc/ssl/private/localhost.key
          name: app-shell-pv-storage
          subPath: Frontend-Https-Certificates/localhost.key
  restartPolicy: OnFailure
  imagePullSecrets:
    - name: dockerhub-secret
